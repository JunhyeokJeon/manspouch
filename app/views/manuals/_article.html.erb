<div class="">
  <% @manuals.each do |manual| %>
    <div class="">
      <h4><%= manual.title %></h4>
      <p><%= manual.category %></p>
      <%= link_to image_tag(manual.image.url(:thumb)), manual_path(manual) %>
    </div>
  <% end %>
</div>
